{
    "name": "calico-k8s-network",
    "type": "calico",
    "etcd_endpoints": "{{ connection_string }}",
    "log_level": "info",
    "ipam": {
        "type": "calico-ipam"
    },
    "policy": {
        "type": "k8s"
    },
    "kubernetes": {
        "kubeconfig": "{{ kubeconfig_path }}"
    }
}
